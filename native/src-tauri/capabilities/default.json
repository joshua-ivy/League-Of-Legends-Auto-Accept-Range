{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC access for the main window so the WebView can call the app's commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
